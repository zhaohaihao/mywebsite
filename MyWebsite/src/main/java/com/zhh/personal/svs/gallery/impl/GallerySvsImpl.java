package com.zhh.personal.svs.gallery.impl;

import org.springframework.core.io.ClassPathResource;
import org.springframework.stereotype.Service;

import com.zhh.common.util.RemoteFileUtil;
import com.zhh.personal.svs.gallery.IGallerySvs;

@Service
public class GallerySvsImpl implements IGallerySvs {
	
	/*public static String imgServerName;
	
	public static String imgServerFolder;
	
	static {
		try {
			org.springframework.core.io.Resource res = new ClassPathResource(proFileName);
			properties.load(res.getInputStream());
			RemoteFileUtil.init(properties.getProperty("imgServerIp"), properties.getProperty("imgServerPort"));
			imgServerName = properties.getProperty("imgServerFolderOA");
			imgServerFolder = properties.getProperty("imgServerFolderOA");
		} catch (Exception e) {
			e.printStackTrace();
		}
	}*/
	
	
}
